'use strict';

/**
 * website-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::website-subscription.website-subscription');
